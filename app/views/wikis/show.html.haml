%h3.col.md-6= markdown @wiki.title
%p= markdown @wiki.body
%br
= link_to "Edit wiki", edit_wiki_path(@wiki), class: 'btn btn-success'
= link_to "Delete wiki", @wiki, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this wiki?'}
= link_to "All wikis", wikis_path(@wikis), class: 'btn btn-default'
= link_to "Add Collaborators", new_wiki_collaborator_path(@wiki), class: 'btn btn-info'
%hr
%table.table-hover.col-md-6
  %tr
    %thead.col-md-6 Collaborators on #{@wiki.title}
    .row
      %th Name
      %th Email
      %th 
    %tr
      - @collaborators.each do |collaborator|
        %td= collaborator.name
        %td= collaborator.email
        %td- if current_user.premium? =link_to "Remove", @collaborator, method: :delete
        %br

